{
  "name": "gloamrun",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Descend the gloam together — a co-op dungeon crawler over peer-to-peer WebRTC.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "trystero": "^0.21.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
